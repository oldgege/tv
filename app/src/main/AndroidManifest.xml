<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.wengyj.tv"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />
    <uses-feature
        android:name="android.software.leanback"
        android:required="true" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@drawable/launcher"
        android:hardwareAccelerated="true"
        android:label="@string/app_name"
        android:theme="@style/Theme.ChineseLearning"
        tools:targetApi="31"
        android:banner="@drawable/launcher">
        <activity
            android:name=".ui.MainActivity"
            android:screenOrientation="landscape"
            android:configChanges="mcc|mnc|orientation|screenSize|keyboard|keyboardHidden|navigation|locale|touchscreen|screenLayout|uiMode|smallestScreenSize|layoutDirection|fontScale"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data
                android:name="android.game.is_game"
                android:value="true" />
        </activity>
    </application>

</manifest>